#include <string>
#include <vector>

using namespace std;

string solution(string myString, string pat) {   
    int i = myString.find_last_of(pat);
    return string(myString.begin(), myString.begin() + i + 1);
}
